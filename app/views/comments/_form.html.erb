<h2>Add a comment:</h2>
<%= form_for([@artwork_iteration, @artwork_iteration.comments.create]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>