<%= form_for(@artwork_iteration) do |f| %>
  <% if @artwork_iteration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artwork_iteration.errors.count, "error") %> prohibited this artwork_iteration from being saved:</h2>

      <ul>
      <% @artwork_iteration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <label for="artwork_iteration_project_name">Project Name</label>
    <input class="form-control" id="artwork_iteration_project_name" name="artwork_iteration[project_name]" type="text">
  </div>
  <div class="form-group">
    <%= f.label :image %>
    <%= f.file_field :image, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
